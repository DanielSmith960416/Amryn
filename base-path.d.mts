/**
 * Types for base-path.mjs, which is plain JavaScript so that next.config.ts
 * and the application bundle can both import the same value. tsconfig sets
 * allowJs: false, so the declaration is written out rather than inferred.
 */
export declare const BASE_PATH: string;
